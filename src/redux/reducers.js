/*
* 根据prevState和action来生成newSteta
* */
import {combineReducers}from 'redux';
function f(prevState={},action) {
    switch (action.type) {
        default:
            return prevState
    }
}
export default  combineReducers({
f
})